{
  "name": "statemachine",
  "version": "0.0.1",
  "description": "Declarative API for building finite state machines",
  "type": "module",
  "types": "./dist/index.d.ts",
  "main": "./dist/index.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "clean": "del-cli ./dist",
    "build": "pnpm run clean && tsc -p ./tsconfig.json",
    "prepack": "pnpm run build",
    "test": "node --experimental-strip-types --test"
  },
  "keywords": [
    "statemachine", 
    "fsm"
  ],
  "author": "Max Burow",
  "license": "MIT",
  "packageManager": "pnpm@10.28.0",
  "devDependencies": {
    "@types/node": "^25.2.3",
    "del-cli": "^7.0.0",
    "typescript": "^5.9.3"
  }
}
